# Demo Mode Configuration
# When enabled, automatically redirects visitors from login to the AI Auditor demo
# and launches an interactive product tour
VITE_DEMO_MODE=false

# API Configuration
VITE_API_URL=http://localhost:5001

# Notes for Demo Mode:
# - Set VITE_DEMO_MODE=true to enable the automatic demo wizard
# - The tour will only run once per browser (uses localStorage flag 'tour_complete')
# - To reset the tour during development, run: window.resetTour() in browser console
# - Or manually clear localStorage: localStorage.removeItem('tour_complete')
